#Auto generated by Edalize

NAME	:= {{ top }}
PARTNAME:= {{ partname }}
BITSTREAM_DEVICE := {{ bitstream_device }}
BUILDDIR:= {{ builddir }}

PCF	:= {{ pcf }}

{% if fasm2bels %}
RR_GRAPH = {{ rr_graph }}
VPR_GRID = {{ vpr_grid }}

all: timing_summary.rpt
{% else %}
all: ${NAME}.bit
{% endif %}

${BUILDDIR}:
	mkdir ${BUILDDIR}

${NAME}.fasm: ${NAME}-nextpnr.mk | ${BUILDDIR}
	$(MAKE) -f ${NAME}-nextpnr.mk

${NAME}.bit: ${NAME}.fasm
	cd ${BUILDDIR} && symbiflow_write_bitstream -d ${BITSTREAM_DEVICE} -f ${NAME}.fasm -p ${PARTNAME} -b ${NAME}.bit

{% if fasm2bels %}
timing_summary.rpt: ${NAME}.bit.v
	bash vivado.sh

${NAME}.bit.v: ${NAME}.bit
	python -m fasm2bels \
		--top ${NAME} \
		--db_root {{ dbroot }}/${BITSTREAM_DEVICE} \
		--part ${PARTNAME} \
		--bitread bitread \
		--bit_file ${NAME}.bit \
		--fasm_file ${NAME}.bit.fasm \
		--eblif ${NAME}.blif \
		--pcf ${PCF} \
		--connection_database channels.db \
		--rr_graph ${RR_GRAPH} \
		--vpr_grid_map ${VPR_GRID} \
		--drive 12 \
		--iostandard LVCMOS33 \
		${NAME}.bit.v ${NAME}.bit.tcl
	rm channels.db

{% endif %}

clean:
	rm -rf ${BUILDDIR}

