#Auto generated by Edalize

TARGET   := {{ name }}

all: $(TARGET).{{ default_target }}

{{ name }}.json: {{ name }}.mk
	$(MAKE) -f $<

{{ name }}.fasm: {{ name }}.json
	nextpnr-{{ arch }} --chipdb {{ chipdb }} --xdc {{ constr }} --json {{ name }}.json --write {{ name }}.routed.json --fasm {{ name }}.fasm --log nextpnr.log {{ additional_options }}

clean:
	rm -f $(TARGET).fasm $(TARGET).routed.json
