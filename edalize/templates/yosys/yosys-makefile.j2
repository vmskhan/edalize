#Auto generated by Edalize

TARGET   := {{ name }}

all: $(TARGET).{{ default_target }}

{% if use_surelog -%}
{{ top }}.uhdm:
	$(MAKE) -f surelog.mk

DEPENDENCIES := {{ top }}.uhdm {{ script_name }}
{% else %}
DEPENDENCIES := {{ script_name }}
{%- endif %}

%.blif: ${DEPENDENCIES}
	uhdm-yosys -l yosys.log -p "tcl {{ script_name }}"
%.json: ${DEPENDENCIES}
	uhdm-yosys -l yosys.log -p "tcl {{ script_name }}"
%.edif: ${DEPENDENCIES}
	uhdm-yosys -l yosys.log -p "tcl {{ script_name }}"

clean:
	rm -f $(TARGET).blif $(TARGET).json
